//I didn't touch this file everything was auto generated when creating the file or added on to by file editor programmer when adding content to other pages
import { TestBed } from '@angular/core/testing';

import { GeonamesService } from './geonames.service';

describe('GeonamesService', () => {
  let service: GeonamesService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GeonamesService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
